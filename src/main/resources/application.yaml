spring:
  application:
    name: qwise
  datasource:
    url: jdbc:postgresql://localhost:5432/qwise_db
    username: guest
    password: guest123
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect
  mail:
    host: smtp.gmail.com
    port: 587
    username: test@gmail.com
    password: "xxxx xxxx xxxx xxxx"
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
jwt:
  expirationMs: 600000

server:
  port: 7775